# ===========================================
# Support Ticket System — Environment Variables
# ===========================================
# 1. Copy this file:   cp .env.example .env
# 2. Fill in values below
# 3. Run:              docker-compose up --build
#
# Two database options are supported:
#   A) Local PostgreSQL  — container spun up by docker-compose (default)
#   B) Supabase          — hosted PostgreSQL, no local db container needed
# ===========================================


# -----------------------------------------------------------
# DJANGO
# -----------------------------------------------------------
DJANGO_SECRET_KEY=django-insecure-change-this-to-a-real-secret-key-50-chars-min
DJANGO_SETTINGS_MODULE=config.settings.production
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,backend


# -----------------------------------------------------------
# LLM — Google Gemini
# Get a free key at: https://aistudio.google.com/apikey
# -----------------------------------------------------------
GEMINI_API_KEY=your-gemini-api-key-here


# -----------------------------------------------------------
# DATABASE — Option A: Local PostgreSQL (docker-compose default)
#
# POSTGRES_HOST=db     <- "db" is the Docker Compose service name.
#                        Docker Compose internal networking resolves it.
# -----------------------------------------------------------
POSTGRES_DB=tickets_db
POSTGRES_USER=tickets_user
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_SSL_MODE=


# -----------------------------------------------------------
# DATABASE — Option B: Supabase (hosted PostgreSQL)
#
# To switch to Supabase:
#   1. Replace the POSTGRES_* values below with your Supabase project creds
#      (found in: Supabase Dashboard -> Project Settings -> Database)
#   2. Set POSTGRES_SSL_MODE=require  (Supabase enforces SSL)
#   3. In docker-compose.yml: comment out the entire "db:" service block
#      and remove the "depends_on: db" condition from the backend service
#   4. Run docker-compose up --build  (no local db container needed)
#
# POSTGRES_DB=postgres
# POSTGRES_USER=postgres.xxxxxxxxxxxx       <- from Supabase connection string
# POSTGRES_PASSWORD=your-supabase-db-password
# POSTGRES_HOST=aws-0-us-east-1.pooler.supabase.com   <- Transaction Pooler URL
# POSTGRES_PORT=6543                        <- 6543 for pooler, 5432 for direct
# POSTGRES_SSL_MODE=require
# -----------------------------------------------------------
